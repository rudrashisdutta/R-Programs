# 7, Write an R-script to create a list having vector, matrix and a list. Now display
# only the 2nd data set of the list.
l <- list(c(1, 2, 3, 45), list("this is a  list memeber", "another", 234), matrix(c(1, 2, 3, 4, 5), nrow = 3))
print(l)